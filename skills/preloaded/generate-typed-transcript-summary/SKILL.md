---
name: generate-typed-transcript-summary
description: 根据已审计的文档分类、知识原子、实体、关系和时间轴，按人物访谈、培训课程、沙龙分享或通用模板生成完整智能总结。适用于不看完整文字稿也需系统学习或为不同内容类型使用专属总结结构时使用。
---

# 生成类型化文字稿总结

该产物服务深入学习，不得用快速概览替代。

## 输入

本 Skill 不直接读取文件，所有数据通过工具调用获取。不修改任何输入数据。

### 通过精读源文档获取

- 段落原文、说话人、时间戳、证据片段
- 语义章节、章节摘要、topic_tags、证据 ID
- 前台聚合章节（可选，用于展示）

### 通过阅读 Wiki 页面获取

- 知识原子（用于反向溯源）
- 实体（用于按人名/机构名检索）
- 关系（可选，用于关联扩展）

其他可用工具：**搜索 Wiki**、**查询知识图谱**、**查看 Wiki 问题**、**标记 Wiki 问题**、**更新 Wiki 问题**、**创建/覆盖 Wiki**

生成前完整阅读 [references/summary-frameworks.md](references/summary-frameworks.md) 和 [references/entity-atom-structures.md](references/entity-atom-structures.md)。

## 工作流程

### 一、类型确定

1. 通过 **精读源文档** 获取段落原文、说话人、时间戳、语义章节和章节摘要；通过 **阅读 Wiki 页面** 获取文档的实体、知识原子和关系。综合两类数据，分析内容的主类型、次类型、分类理由和置信度。
2. 主类型置信度不足时采用 `general`，同时保留可能的候选类型；不得强套专属模板。

### 二、内容组织

3. 通过 **搜索 Wiki** 交叉验证同分类实体和补充结构维度。
4. 按对应框架的标准二级标题和顺序组织知识；标题文案不得自由改写，只可在标准标题下增加三级小节。利用知识原子的结构维度组织正文：方法论的 `steps`/`criteria` 直接支撑方法章节，案例的 `context`/`outcome` 支撑案例章节，概念的 `definition`/`distinction` 支撑概念解释，洞察的 `claim`/`qualifications` 支撑观点呈现和适用边界。
5. 明确区分原文观点、忠实概括、跨段归纳和分析推断。

### 三、输出

6. 调用 **创建/覆盖 Wiki** 工具创建新 Wiki 页面承载总结，并写入知识库。Wiki 页面命名为"文档名称_知识总结"。写入契约：工具 `page_type` 用 `index`；页面 frontmatter 必须含 `type: typed_summary`、`source_video_id: {视频ID}` 与 `transcript_generation: {转写代次}`。由 vidsage 内容流水线触发时，页面 slug 固定为 `typed-summary/{视频ID}`，不得使用视频标题或其他产物 slug。

### 四、审计

7. 校验总结：确认主类型模板的二级标题和顺序完整；审计失败时不得将总结标记为完成。
8. 审计发现事实缺失、来源不足或模板偏差时，通过 **标记 Wiki 问题** 在总结页面标记问题详情。
9. 审计通过后，通过 **更新 Wiki 问题** 将上游遗留问题状态更新为已解决。

## 强制规则

- 总结长度由实质知识量决定，不设统一字数下限来鼓励注水。
- 不得把个人经验改写为普遍建议。
- 模板字段没有来源支持时应省略或说明缺失，不得补齐套话。
- 标准二级标题是输出契约，不是选题建议。某章证据不足时保留标题并简明说明缺失，不得删除、合并或改名该章。
- 主类型模板决定二级章节；次类型内容只能作为相关章节下的三级小节，不能改写主骨架。
- 不得将审计结果作为输出内容
